version: "3.8"
services:
  anime-rec:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data:ro
      - ./.cache:/root/.cache
      - ./.streamlit-cache:/root/.cache/streamlit
    environment:
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
      - STREAMLIT_SERVER_PORT=8501